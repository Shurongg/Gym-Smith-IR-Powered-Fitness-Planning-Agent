# Copy this file to .env and fill in your values.
# The frontend collects the API key at runtime, so this file is optional.

# OpenAI API key — only needed if running the backend standalone (not via the UI)
OPENAI_API_KEY=sk-...
